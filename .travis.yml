## Travis CI Integration

language: c

install:
  - sudo apt-get install mono-devel mono-gmcs nunit-console
script:
  - xbuild
  - nunit-console Terradue.ServiceModel.Ogc.OwsContext.Test/bin/Terradue.ServiceModel.Ogc.OwsContext.Test.dll
